module.exports =
function clozeCard(text, cloze){
    this.fullText = text;
    this.cloze = cloze;
    this.partial = ()=>{
        //some function to show text minus cloze
    }
    //throw an error if cloze does not appear in text 
};